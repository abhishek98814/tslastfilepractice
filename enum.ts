// enum in number behave default as increment but it is not compulsory
// we can put string too and string number too

enum  sheera{
    Aisle = 90,
    Middle,
    window,
    lastVal = 99
}

// the valur of window if 92 
// but if i declare myself the value will obviously change 


enum lastDecalaration {
    first="Abhishek",
    second=90
}